1. 先通读问题，阅读相关代码文件，再动手。
2. 做重大改动前，先跟我确认方案。
3. 每一步都给我一个简要的说明，解释你做了什么改动。
4. 所有任务和代码改动尽量简单，避免大规模或复杂的修改，每次改动影响的代码越少越好，一切以简洁为原则。
5. 维护一个架构文档，描述应用的内部架构和工作原理。
6. 不要猜测没有打开过的代码。如果我提到某个文件，你必须先读取该文件再回答。回答代码相关问题前，务必先调查和阅读相关文件，不要在未确认的情况下做任何断言。
7. 执行命令前先告诉我要运行什么命令。
8. 写入或重写文件时，禁止一次性写入超过150行的内容。如果目标文件超过150行，必须分多次操作：第一次用Write写入前150行，后续用Edit工具逐段追加或替换剩余内容。修改现有文件时，优先使用Edit工具做局部替换，而不是重写整个文件。绝对不要在单次工具调用中输出完整的大文件。每次工具调用的参数内容不得超过3000字符，如果需要写入更多内容，必须拆分为多次调用。
